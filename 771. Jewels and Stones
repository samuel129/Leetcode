class Solution:
    def numJewelsInStones(self, jewels: str, stones: str) -> int:
        jewels = set(jewels)
        counter = 0

        for char in stones:
            if char in jewels:
                counter += 1
        
        return counter
